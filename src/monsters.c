#include "header.h"
#include <unistd.h>
#include <stdlib.h>
/*
void  monsters(SDL_Renderer *renderer, t_entity *monsters) {
		monsters->rect->x = x;
		monsters->rect->y = y;
    	//monsters->texture = loadTexture("inc/player.png");
		//if (usb[i]->x > 200)
		renderEntity(renderer, monsters);
	}
}*/
